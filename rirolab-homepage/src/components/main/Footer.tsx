import React from 'react';
import styles from 'src/styles/main/Footer.module.css'

const Footer: React.FC = () => {
  return (
    <footer className={styles.footer}>
    </footer>
  );
};

export default Footer;


You can use a GIF file smaller than 4mb. 
You can use an image file smaller than 250kb.

You can reduce the size of image files using the "convert" command of ImageMagic in Ubuntu.

~~~~
convert source.png -resize 512 target.png
~~~~